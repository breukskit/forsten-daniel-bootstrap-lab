Project build with bootstrap 4. The very little customization that is happening can be found in main.scss.

To install the depencdencies needed (jquery, popper.js and bootstrap.js), run the following command:

npm install
